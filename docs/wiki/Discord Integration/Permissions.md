# Permissions

This page describes how SST permissions map across:

- SST users/roles (admin/manager/viewer)
- API authorization (JWT)
- API key (server authorization)
- Discord roles (if integrated)

If you add Discord integration, keep the same core roles and layer Discord on top.
