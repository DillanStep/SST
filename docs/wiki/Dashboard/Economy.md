# Economy

The Economy view combines:

- trade logs (Expansion Market)
- spawn/type data (types.xml)

## Backing API endpoints

- `GET /economy`
- `GET /economy/spawn-data`
- `GET /archive/trades/stats` (if archiving enabled)
