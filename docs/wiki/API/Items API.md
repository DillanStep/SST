# Items API

Items endpoints serve a cached searchable list of spawnable items.

Common endpoints:

- `GET /items`
- `GET /items/search`
- `GET /items/categories`
- `GET /items/:className`
- `POST /items/refresh`
- `GET /items/inventory-counts`

Backing files:

- `paths.api/server_items.json`
