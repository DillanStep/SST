# Players API

This area covers player identity, online status, and player-specific data.

Common endpoints:

- `GET /online`
- `GET /online/active`
- `GET /online/:playerId`
- `GET /dashboard/player/:playerId`
- `GET /positions/:playerId`

Related mod exports:

- online players JSON
- position tracking DB (API-side)
